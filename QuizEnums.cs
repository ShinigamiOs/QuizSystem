namespace QuizSystem{
    /// <summary>
    /// Enum que representa el resultado de una respuesta.
    /// </summary>
    public enum AnswerResult{
        Correct,
        Incorrect,
        Partial,
        NotAnswered
    }
}